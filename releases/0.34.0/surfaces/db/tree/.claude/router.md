<!-- nina:slot db.1 -->

### DBA is a guardrail, not a stage
Invoke **dba** any time the schema, a migration or a query is touched — regardless of where you are in the pipeline. No such change reaches qa without dba approval; the reviewer names it on its Gates line.
