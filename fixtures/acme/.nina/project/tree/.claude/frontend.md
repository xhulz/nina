<!-- nina:slot project.1 conventions -->
- Screens are server-rendered pages with islands of client code; a component fetches nothing on its own.
- Styling is the design tokens in `packages/ui`; nothing is styled inline.
