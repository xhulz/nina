/** How long a session may be idle before the user must sign in again, in minutes. */
export const SESSION_IDLE_MINUTES = 30;

/** The most notes one page of a listing returns. */
export const PAGE_SIZE = 50;
